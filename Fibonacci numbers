def fibonacci(n): 
	a = 0
	b = 1
	print(a)
	print(b)
	if n < 0: 
		print("no negitive numbers") 
	elif n == 0: 
		return a 
	elif n == 1: 
		return b 
	else: 
		for i in range(2,n): 
			c = a + b 
			print(c)
			a = b 
			b = c
      
fibonacci(30)
